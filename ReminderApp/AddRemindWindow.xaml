<Window x:Class="ReminderApp.AddRemindWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReminderApp"
        mc:Ignorable="d"
        Title="Новое напоминание" Height="550" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="White" WindowStyle="ToolWindow"
        FontSize="14"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Стили (идентичны AddTaskWindow) -->
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#2564CF"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2564CF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#2564CF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#2564CF"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2564CF"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название напоминания -->
        <TextBlock Grid.Row="0" Text="Название напоминания" 
                   FontSize="16" Margin="0,0,0,8" Foreground="#333333"/>
        <TextBox Grid.Row="1" Name="RemindTitleTextBox" FontWeight="Bold"/>

        <!-- Дата -->
        <TextBlock Grid.Row="2" Text="Дата" 
                   FontSize="16" Margin="0,20,0,8" Foreground="#333333"/>
        <DatePicker Grid.Row="3" Name="RemindDateDatePicker" 
                    SelectedDateFormat="Short"
                    FirstDayOfWeek="Monday"
                    Loaded="RemindDateDatePicker_Loaded"/>

        <!-- Время -->
        <TextBlock Grid.Row="4" Text="Время (чч:мм)" 
                   FontSize="16" Margin="0,20,0,8" Foreground="#333333"/>
        <ComboBox Grid.Row="5" Name="RemindTimeComboBox" 
                  Height="40" IsEditable="True" Text="12:00"
                  Loaded="RemindTimeComboBox_Loaded"/>

        <!-- Блок выбора повтора -->
        <TextBlock Grid.Row="6" Text="Повторять" 
                   FontSize="16" Margin="0,20,0,8" Foreground="#333333"/>
        <StackPanel Grid.Row="7" Margin="0,0,0,10">
            <!-- Радиокнопки -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <RadioButton GroupName="RepeatGroup" Name="NoRepeatRadio" 
                             Content="Без повтора" IsChecked="True"/>
                <RadioButton GroupName="RepeatGroup" Name="WeekDaysRadio" 
                             Content="По дням недели"/>
            </StackPanel>

            <!-- Дни недели (чекбоксы) -->
            <StackPanel Orientation="Horizontal" Name="DaysPanel">
                <CheckBox Content="Пн" Tag="Monday"/>
                <CheckBox Content="Вт" Tag="Tuesday"/>
                <CheckBox Content="Ср" Tag="Wednesday"/>
                <CheckBox Content="Чт" Tag="Thursday"/>
                <CheckBox Content="Пт" Tag="Friday"/>
                <CheckBox Content="Сб" Tag="Saturday"/>
                <CheckBox Content="Вс" Tag="Sunday"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Width="100" 
                    Background="#F5F5F5" Foreground="#333333"
                    Margin="0,0,15,0" Click="CancelButton_Click"/>
            <Button Content="Сохранить" Width="120" 
                    Background="#2564CF" Foreground="White"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>